# =============================================================================
# ASC Inventory Production Environment Variables
# =============================================================================
#
# Copy this file to .env and fill in the values.
# The .env file is gitignored and must be created on the droplet.
#
# Location on droplet: /home/tim/asc-inventory/.env
# =============================================================================

# REQUIRED: PostgreSQL database password
POSTGRES_PASSWORD=your-secure-password-here

# REQUIRED: JWT signing secret (use a long random string)
JWT_SECRET=your-jwt-secret-here

# REQUIRED: Docker image version to deploy
# Update this value and run: docker compose -f docker-compose.prod.yml pull && up -d
IMAGE_TAG=1.5.6
