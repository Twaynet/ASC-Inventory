beta.orthowise.dev {
	tls /etc/caddy/certs/origin.pem /etc/caddy/certs/origin-key.pem

	header * X-Origin droplet

	handle_path /api/* {
		reverse_proxy api:3001
	}

	handle {
		reverse_proxy web:3000
	}
}
